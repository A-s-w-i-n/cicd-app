name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted  # Use the GitHub self-hosted runner on your EC2 instance
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'  

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Deploy with PM2
        run: |
          pm2 restart backend server || pm2 start src/index.js backendserver
          pm2 save
